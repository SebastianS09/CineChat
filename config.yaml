llm:
  model: "gpt-4o-mini"
  base_url: "https://api.openai.com/v1"
  api_key: ""        # or set LLM_API_KEY env var

gradium:
  api_key: ""        # or set GRADIUM_API_KEY env var

session:
  silence_timeout_s: 0.0
  assistant_speaks_first: true

# Google Maps — used server-side only for geocoding (/api/geocode)
# Set via env var: GOOGLE_MAPS_API_KEY=AIza...
